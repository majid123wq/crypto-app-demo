# Frontend Environment Variables
# These are build-time variables; prefix with REACT_APP_

# API Base URL (should point to backend). In Netlify set to: https://<your-render-backend>.onrender.com/api
REACT_APP_API_URL=https://your-backend.onrender.com/api

# Environment
NODE_ENV=development
